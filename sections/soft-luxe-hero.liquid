{% comment %} Homepage hero for Launch Offer {% endcomment %}
<section class="slx-hero" aria-labelledby="slx-hero-title">
  <div class="slx-hero__inner">
    <h1 id="slx-hero-title">{{ variables.CAMPAIGN_TITLE | default: 'Soft. Luxe. Yours' }}</h1>
    <p class="slx-hero__sub">Launch Offer — <strong>{{ variables.DISCOUNT_PERCENT | default: 50 }}% OFF Storewide</strong></p>
    <p class="slx-hero__meta">Discount <strong>applies automatically</strong> in cart & checkout</p>
    <div class="slx-hero__ctas">
      <a class="slx-btn" href="/collections/all">Shop the Launch</a>
    </div>
    <div class="slx-countdown" data-slx-countdown></div>
  </div>
</section>

{% schema %}
{
  "name": "Soft Luxe Hero",
  "settings": [],
  "presets": [
    { "name": "Soft Luxe Hero" }
  ]
}
{% endschema %}
